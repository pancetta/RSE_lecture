# Additional dependencies for Lecture 10
# Install on top of base environment.yml
# Lecture 10 uses only base dependencies (no additions needed - collaboration tools covered conceptually)
name: rse_lecture
channels:
  - conda-forge
dependencies: []
