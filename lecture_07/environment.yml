# Additional dependencies for Lecture 7
# Install on top of base environment.yml
# Lecture 7 uses only base dependencies (no additions needed for debugging/profiling)
name: rse_lecture
channels:
  - conda-forge
dependencies: []
