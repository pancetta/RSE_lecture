name: rse_lecture
channels:
  - conda-forge
dependencies:
  # Lecture 12 has no additional Python dependencies beyond the base environment
  # Workflow tools (Snakemake, Nextflow, etc.) are typically installed separately
  # and not required to run this lecture notebook
  # Students learn concepts that work with their existing environment
