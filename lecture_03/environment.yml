# Additional dependencies for Lecture 3
# Install on top of base environment.yml
# Lecture 3 uses only base dependencies (no additions needed)
name: rse_lecture
channels:
  - conda-forge
dependencies: []
