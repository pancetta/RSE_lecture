# Additional dependencies for Lecture 1
# Install on top of base environment.yml
# Lecture 1 uses only base dependencies (no additions needed)
name: rse_lecture
channels:
  - conda-forge
dependencies: []
